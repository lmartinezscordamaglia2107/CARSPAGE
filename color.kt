package com.example.carspage

import androidx.compose.ui.graphics.Color

// ==========================================
// PALETA DE CORES PRINCIPAL (CarsPage)
// ==========================================

// Tons de Destaque (Brand / Accent Colors)
val CarAccentPrimary = Color(0xFF2563EB)   // Azul Esportivo / Elétrico (Destaque principal)
val CarAccentSecondary = Color(0xFF3B82F6) // Azul mais claro para estados de foco/hover
val CarAccentVibrant = Color(0xFFEF4444)   // Vermelho Pinça de Freio / Alertas

// Tons Escuros (Dark Mode / Superfícies)
val CarDarkBackground = Color(0xFF0F172A)  // Cinza Asfalto Profundo (Fundo Dark)
val CarDarkSurface = Color(0xFF1E293B)     // Superfície de Cards e Menus Dark
val CarDarkText = Color(0xFFF8FAFC)        // Texto claro sobre fundo escuro

// Tons Claros (Light Mode / Superfícies)
val CarLightBackground = Color(0xFFF8FAFC) // Fundo Light limpo
val CarLightSurface = Color(0xFFFFFFFF)    // Superfície de Cards e Menus Light
val CarLightText = Color(0xFF0F172A)       // Texto escuro sobre fundo claro

// Tons de Suporte e Bordas
val CarGrayChassis = Color(0xFF64748B)     // Cinza médio para textos secundários/subtítulos
val CarBorderLine = Color(0xFFCBD5E1)      // Linhas de divisão e bordas (Light)
val CarBorderLineDark = Color(0xFF334155)  // Linhas de divisão e bordas (Dark)

// ==========================================
// CORES PADRÃO DO JETPACK COMPOSE (Opcional/Fallback)
// ==========================================
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)
